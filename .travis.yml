---
language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - pip install ansible
  - ansible-playbook -i hosts site.yml -v

script:
  - su haas
  - haas list_projects

